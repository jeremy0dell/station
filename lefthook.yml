pre-commit:
  commands:
    biome:
      run: node scripts/run-without-git-locals.mjs pnpm lint

pre-push:
  commands:
    test:
      run: node scripts/run-without-git-locals.mjs pnpm gate:pre-push {push_files}
